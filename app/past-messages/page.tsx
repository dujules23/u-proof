import DefaultLayout from "@/components/layout/DefaultLayout";
import { FC } from "react";

interface Props {}

const PastMessages: FC<Props> = (props): JSX.Element => {
  return <DefaultLayout>PastMessages</DefaultLayout>;
};

export default PastMessages;
